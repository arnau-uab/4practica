#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main (int argc, char *argv[]){
	bool actiu = True;

	char cadena = argv[1];
	fd = open(./cadena, O_WRONLY);

	if (fd == -1) {
    		perror("Error: no s'ha pogut crear un arxiu");
		exit(-1);
	}

	char *array[]

	while(actiu){
		char linia = scanf();
		for(int i = 0; i>=0; i++){
			array[i] = linia
		}
		for(int j = 0; i>=0; i++){
			write(fd, void j, );

